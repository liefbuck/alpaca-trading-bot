# Pinned to the known-good environment (Python 3.14 / pythoncore) as of 2026-06-08
# so a fresh `pip install -r requirements.txt` on a new machine or after a Python
# reinstall can't pull a breaking new major version. Bump deliberately, then run
# `python tests/test_all.py` and re-verify a live scan before trusting it.
alpaca-py==0.43.4
python-dotenv==1.2.2
requests==2.34.2
yfinance==1.3.0
schedule==1.2.2
pandas==3.0.3
pytz==2026.2
lxml==6.1.0

# Only used by the archived Flask dashboard (_archived/dashboard.py). The live
# dashboard is serve.py + dashboard.html on the stdlib http.server, so this is
# not needed to run the bot, but it is pinned for completeness.
flask==3.1.3
